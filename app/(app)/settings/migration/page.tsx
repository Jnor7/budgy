"use client";
import { ArrowLeft,CheckCircle2,FileArchive,RefreshCcw,ShieldCheck,Upload } from "lucide-react";import Link from "next/link";import { useState } from "react";import { Card } from "@/components/ui/card";import { readBudgetJrArchive,mergeImportedData,type ArchivePreview } from "@/features/migration/importer";import { useBudgyData } from "@/lib/data/data-provider";import { deleteAttachmentFile,uploadAttachmentFile } from "@/services/attachments";
export default function MigrationPage(){const {data,replaceAll,importArchive,localMode,userId}=useBudgyData();const [preview,setPreview]=useState<ArchivePreview>();const [error,setError]=useState("");const [report,setReport]=useState("");const [busy,setBusy]=useState(false);const select=async(file:File)=>{setError("");setReport("");try{setPreview(await readBudgetJrArchive(file));}catch(reason){setError(reason instanceof Error?reason.message:"Archive invalide.");}};const execute=async()=>{if(!preview)return;setBusy(true);setError("");const incoming=structuredClone(preview.data);const uploaded:string[]=[];try{for(const item of preview.attachmentFiles){const attachment=incoming.attachments.find((entry)=>entry.id===item.attachmentId);if(!attachment)continue;attachment.storagePath=await uploadAttachmentFile(item.file,userId,localMode);attachment.sizeBytes=item.file.size;attachment.mimeType=item.file.type||attachment.mimeType;uploaded.push(attachment.storagePath);}if(localMode){const result=mergeImportedData(data,incoming);replaceAll(result.data);setReport(`${result.inserted} éléments importés · ${result.skipped} doublons ignorés.`);}else{const result=await importArchive(incoming,preview.checksum);if(result.alreadyImported)for(const path of uploaded)await deleteAttachmentFile(path,false).catch(()=>undefined);setReport(result.alreadyImported?"Cette archive a déjà été importée. Aucun doublon créé.":`${result.inserted} éléments importés · ${result.skipped} doublons ignorés${result.batchId?` · lot ${result.batchId.slice(0,8)}`:""}.`);}}catch(reason){for(const path of uploaded)await deleteAttachmentFile(path,localMode).catch(()=>undefined);setError(reason instanceof Error?reason.message:"Import impossible.");}finally{setBusy(false);}};return <main className="page page-narrow stack"><div className="spread"><Link className="icon-button" href="/"><ArrowLeft/></Link><strong>Migration Budget JR</strong><span className="icon-button"><ShieldCheck className="positive"/></span></div><div className="bubble-header"><h1 style={{fontSize:30}}>Importer mes données</h1><p>Budget JR → Budgy</p></div><Card><div className="row"><span className="icon-tile icon-purple"><FileArchive/></span><div><strong>budget-jr-export.zip</strong><div className="muted small">Manifest v1, 21 entités et pièces jointes</div></div></div><label className="button button-primary" style={{width:"100%",marginTop:14}}><Upload size={17}/>Choisir l’archive<input type="file" accept=".zip,application/zip" hidden onChange={(e)=>{const file=e.target.files?.[0];if(file)void select(file);}}/></label>{error&&<p className="error">{error}</p>}</Card>{preview&&<Card><div className="spread"><h2 className="section-title">Résumé avant import</h2><span className="button button-soft">v{preview.manifest.version}</span></div><div className="stack-sm">{Object.entries(preview.counts).map(([key,count])=><div className="spread" key={key}><span className="muted">{key}</span><strong>{count}</strong></div>)}</div>{preview.attachmentFiles.length>0&&<p className="positive small">{preview.attachmentFiles.length} fichier(s) prêt(s) à être transféré(s).</p>}{preview.warnings.map((warning)=><p className="error" key={warning}>{warning}</p>)}<button className="button button-primary" disabled={busy} style={{width:"100%",marginTop:16}} onClick={()=>void execute()}><CheckCircle2 size={18}/>{busy?"Import en cours…":"Importer dans Budgy"}</button></Card>}{report&&<Card><div className="row"><CheckCircle2 className="positive"/><strong>{report}</strong></div><p className="muted small">L’import est idempotent grâce au checksum et aux legacy_id. Relancer la même archive n’ajoute pas les mêmes éléments.</p></Card>}<Card><div className="row"><RefreshCcw className="accent"/><div><strong>Import récupérable</strong><div className="muted small">Aucune donnée existante n’est effacée. Les doublons sont ignorés et le rapport reste visible.</div></div></div></Card></main>;}
