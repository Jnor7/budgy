"use client";
import { Check,ChevronRight,Search } from "lucide-react";import { useMemo,useState } from "react";import { airports,searchAirports } from "@/lib/airports/airports";import { Sheet } from "@/components/ui/modal";
export function AirportPicker({open,title,value,onClose,onSelect}:{open:boolean;title:string;value:string;onClose:()=>void;onSelect:(code:string)=>void}){const [query,setQuery]=useState("");const results=useMemo(()=>searchAirports(query),[query]);const selected=airports.find((x)=>x.code===value);return <Sheet open={open} title={title} onClose={onClose}><div className="stack"><div className="card row"><Search className="muted"/><input className="input" style={{border:0,background:"transparent",padding:0}} value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="Rechercher une ville, un code, un pays…"/></div>{selected&&<div><div className="muted small" style={{marginBottom:8}}>Sélection actuelle</div><div className="card row" style={{borderColor:"#9f80f4"}}><strong className="accent" style={{fontSize:31}}>{selected.code}</strong><div className="list-main"><strong>{selected.flag} {selected.city}</strong><div className="muted small">{selected.name}<br/>{selected.country}</div></div><span className="status-dot active"><Check size={15}/></span></div></div>}<div><div className="muted small" style={{marginBottom:8}}>{query?"Résultats":"Aéroports populaires"}</div>{results.map((airport)=><button className="card list-row" style={{marginBottom:9,padding:14}} key={`${airport.code}-${airport.name}`} onClick={()=>{onSelect(airport.code);setQuery("");onClose();}}><strong style={{fontSize:25,minWidth:62}}>{airport.code}</strong><div className="list-main"><strong>{airport.flag} {airport.city}</strong><span className="muted small">{airport.name} · {airport.country}</span></div><ChevronRight className="muted"/></button>)}</div></div></Sheet>;}
