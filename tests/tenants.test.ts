import { describe,expect,it } from "vitest";import { carryOverForMonth,totalDueForMonth } from "@/lib/domain/tenants";import type { RentPayment,Tenant,TenantDebt } from "@/types/domain";
const tenant:Tenant={id:"t",userId:"u",name:"A",monthlyRent:500,dueDay:5,note:"",createdAt:"2023-01-01T12:00:00Z"};
const payment=(year:number,month:number,amountReceived:number):RentPayment=>({id:`${year}-${month}`,userId:"u",tenantId:"t",year,month,amountDue:500,amountReceived,carryOver:0,isPaid:amountReceived>=500,note:""});
describe("reports loyers",()=>{const recentTenant={...tenant,createdAt:"2026-04-01T12:00:00Z"};it("calcule un mois normal",()=>expect(totalDueForMonth(recentTenant,[payment(2026,4,500)],[],2026,5)).toBe(500));it("reporte un paiement partiel",()=>expect(carryOverForMonth(recentTenant,[payment(2026,4,300)],[],2026,5)).toBe(200));it("ajoute une dette impayée",()=>{const debts:TenantDebt[]=[{id:"d",userId:"u",tenantId:"t",label:"Charge",amount:80,month:5,year:2026,isPaid:false,createdAt:"2026-05-01T12:00:00Z"}];expect(totalDueForMonth(recentTenant,[payment(2026,4,500)],debts,2026,5)).toBe(580);});it("borne la récursion à 36 mois",()=>expect(()=>carryOverForMonth(tenant,[],[],2026,5)).not.toThrow());});
